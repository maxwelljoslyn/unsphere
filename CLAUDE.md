# Project instructions

## Python
- Use `uv` for all Python-related commands (e.g. `uv run pytest`, `uv run python ...`). Do not call `python`/`pip` directly or the `.venv` interpreter by path.
- Run tests with `uv run pytest -n auto`. Always run tests in a Haiku subagent.
